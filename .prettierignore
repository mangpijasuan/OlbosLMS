node_modules
dist
.next
build
coverage
pnpm-lock.yaml
packages/database/prisma/migrations
**/generated

# Regenerated by `next build` with its own quote style. Next.js states the file
# must not be edited, so formatting it only guarantees a spurious diff after
# every web build.
apps/web/next-env.d.ts
